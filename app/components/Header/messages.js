import { defineMessages } from 'react-intl';
import { scope } from '../../i18n';

export default defineMessages({
  logged_in_as: {
    id: `${scope}.logged_in_as`,
    defaultMessage: 'Logged in as',
  },
  log_in: {
    id: `${scope}.log_in`,
    defaultMessage: 'Log in',
  },
  log_out: {
    id: `${scope}.log_out`,
    defaultMessage: 'Log out',
  },
});
