import { defineMessages } from 'react-intl';
import { scope } from '../../i18n';

export default defineMessages({
  search_hint: {
    id: `${scope}.search_hint`,
  },
  search: {
    id: `${scope}.search`,
  },
  search_term: {
    id: `${scope}.search_term`,
  },
  search_form_legend: {
    id: `${scope}.search_form_legend`,
  },
});
